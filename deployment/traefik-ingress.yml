# Ingress 文件如下
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: traefik-ingress
spec:
  rules:
  - host: test
    http:
      paths:
      - path: /test1
        backend:
          serviceName: "test1.test"
          servicePort: 3000
      - path: /test2
        backend:
          serviceName: "test2.test"
          servicePort: 3000          
 